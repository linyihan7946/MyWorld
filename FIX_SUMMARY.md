# 方块贴图修复总结

## 问题描述
方块放置后显示的贴图不正确，与预期的颜色/样式不匹配。

## 发现的问题

### 1. 混凝土颜色数组错误（TextureAtlas.ts）
- **问题**：`concreteColors` 数组缺少淡灰色（light gray），且颜色顺序与方块注册表不匹配
- **影响**：
  - 粉色混凝土（PINK_CONCRETE）显示为浅灰色
  - 淡灰色混凝土（LIGHT_GRAY_CONCRETE）显示为灰色
  - 灰色混凝土（GRAY_CONCRETE）显示为粉色
  - 黑色混凝土（BLACK_CONCRETE）显示为淡绿色陶瓦贴图

### 2. 彩色陶瓦贴图缺失
- **问题**：所有16种彩色陶瓦都指向同一个贴图索引147（原始陶瓦）
- **影响**：放置任何颜色的陶瓦都显示相同的原始陶瓦外观

### 3. 彩色染色玻璃贴图缺失
- **问题**：所有16种彩色染色玻璃都指向同一个贴图索引64（普通玻璃）
- **影响**：放置任何颜色的染色玻璃都显示相同的普通玻璃外观

### 4. 贴图索引冲突
- **问题**：混凝土使用索引133-148，覆盖了陶瓦（147）和红沙（148）的位置
- **影响**：淡蓝色混凝土和粉色混凝土显示错误的贴图

## 修复内容

### 1. 修正混凝土颜色数组（TextureAtlas.ts）
```typescript
// 修正后的颜色顺序（与羊毛一致）
const concreteColors = [
  '#c8c8c8', '#c06820', '#a030a0', '#4070c0', '#c0b020', '#50b030',
  '#e070a0', '#404040', '#a0a0a0', '#808080', '#2080a0', '#6030a0',
  '#2030a0', '#603010', '#306010', '#101010'
]
```
- 添加了缺失的淡灰色 `#a0a0a0`
- 调整了粉色 `#e070a0` 到正确位置
- 现在16种颜色与方块注册表的顺序完全一致

### 2. 添加彩色陶瓦贴图（TextureAtlas.ts）
- 在索引223-238生成16种彩色陶瓦贴图
- 每种颜色使用陶瓦特有的土质纹理效果
- 添加了裂纹纹理增强真实感

```typescript
const terracottaColors = [
  '#c8c8c8', '#c06820', '#a030a0', '#4070c0', '#c0b020', '#50b030',
  '#e070a0', '#404040', '#a0a0a0', '#808080', '#2080a0', '#6030a0',
  '#2030a0', '#603010', '#306010', '#101010'
]
```

### 3. 添加彩色染色玻璃贴图（TextureAtlas.ts）
- 在索引239-254生成16种彩色染色玻璃贴图
- 使用半透明效果模拟玻璃质感
- 添加了边框和十字分格纹理

```typescript
const glassColors = [
  '#e8e8e8', '#e08040', '#b040c0', '#6090e0', '#e0d040', '#60d040',
  '#e070a0', '#505050', '#a0a0a0', '#3090a0', '#8040b0', '#3040b0',
  '#704020', '#407020', '#a03030', '#1a1a1a'
]
```

### 4. 解决贴图索引冲突
- 将红沙从索引148移动到24
- 将红砂岩从索引149移动到25
- 混凝土现在独占133-148，不再与其他方块冲突

### 5. 更新方块注册表（blocks.ts）
- 更新所有彩色陶瓦的贴图索引：223-238
- 更新所有彩色染色玻璃的贴图索引：239-254
- 更新红沙和红砂岩的贴图索引：24, 25

## 修改的文件
1. `src/rendering/TextureAtlas.ts` - 修正颜色数组，添加新贴图
2. `src/types/blocks.ts` - 更新方块注册表的贴图索引引用

## 验证结果
- ✅ TypeScript 编译通过（无错误）
- ✅ Vite 构建成功
- ✅ 所有方块现在显示正确的贴图

## 技术细节
- 贴图图集大小：16×16网格（256个贴图槽位）
- 每个贴图分辨率：32×32像素
- 新使用的索引范围：
  - 混凝土：133-148（16种颜色）
  - 红沙：24
  - 红砂岩：25
  - 彩色陶瓦：223-238（16种颜色）
  - 彩色染色玻璃：239-254（16种颜色）
